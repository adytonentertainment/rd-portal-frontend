# Ralph Progress Log
Started: Thu Jun 11 17:43:15 CEST 2026
---

## Codebase Patterns
- Dev server: http://localhost:3001, compile log at /tmp/verax_rd_frontend.log. Admin routes under /admin, gated by useIsAdmin() (src/utils/auth.js hardcoded ADMIN_EMAILS).
- Demo/mock state: src/mocks/distributionState.js + localStorage 'rd_distribution_state_v1' (subscribe(listener) pattern). RedZed (id -5) uses real data via src/mocks/redZedRealData.js. NEVER break the mock path — live behavior only behind REACT_APP_STATEMENTS_LIVE via src/config/featureFlags.js.
- Persona system: src/utils/persona.js, /persona/:id routes.
- Backend under parallel construction at /Users/stevengarcia/VERAX_2/verax_backend branch ralph/statement-data-integration — check its app/routers/ + scripts/ralph/prd.json to see which endpoints are real yet; PRD §9 is the contract otherwise. Real fixture files + expected_values.json: verax_backend/tests/fixtures/statements/.
- Statement domain vocabulary (PRD §2): period codes PUB25H2/PUB26H1/PUB25Q4/PUB26Q2; catalogs MECH/YT; account codes like CSJ002/JN0249/C00650/C00739-New; zero payable is NORMAL (threshold_carryover/recouped/zero_earnings) and must be explained, not alarmed.
- Pre-existing uncommitted demo WIP must be committed once as its own chore commit, never reverted (see scripts/ralph/CLAUDE.md).
- Lint gate: `npm run lint` runs `eslint .` over the whole repo. `.eslintignore` (added in FE-001) excludes build/, coverage/, public/ (vendored minified pdf.worker + sw.js), and dead src/components/Globe/Globe_Pins_NEW.js — WITHOUT it, eslint grinds through the 92MB build/ dir for 30+ min and never finishes. Exit 0 = 0 errors; the ~82 remaining warnings are pre-existing and don't fail the gate.
- Prettier is enforced via eslint (singleQuote, semi). Run `npx eslint <paths> --fix` on new files before the full lint to fix formatting fast.
- Test files need `/* eslint-env jest */` at the top — .eslintrc has no jest env.
- Mocking axios in tests: `jest.mock('axios', () => ({ __esModule: true, default: jest.fn() }))` avoids axios v1 ESM transform issues under CRA jest; then `axios.mockResolvedValue(...)` works directly.
- API client (src/api/statementsAdmin.js): axios({...}) with `${REACT_APP_BACKEND_URL}` prefix (trailing slashes stripped), Bearer token from localStorage 'token' (same as Revenue.jsx). All functions reject with normalized {status, message, detail}; status 0 = backend unreachable (drive the "backend unreachable" banners off that).
- Client paths follow the FE-001 story notes (backend router not built yet): /admin/statements/uploads, /admin/statements/batches/{id}, /admin/findings/{id}/waive|acknowledge, /admin/statements/{id}/lines?page=&page_size=. Re-check verax_backend/app/routers/ each iteration and reconcile shapes when it lands.
- /tmp/verax_rd_frontend.log did not exist during FE-001 even though the dev server serves 200 on :3001 — verify compile health via curl + (if needed) npm run build; don't trust the log path blindly. (Still missing during FE-002 — used npm run build as the compile gate.)
- Backend upload contract (REAL, live since backend US-003/US-007): POST /admin/statements/uploads → 202 {upload_id, file_count, status}; GET /admin/statements/uploads/{id} → {upload_id, status, stage, file_count, uploaded_at, stats}. status enum: uploaded|sorting|parsing|validating|done|failed. stats shape: {received, stored, skipped:[names], sort:{batches, statements, statement_ids, paired, unpaired:[names], unparseable:[names], duplicates:[names]}, parse:{total, parsed, failed, remaining}, validate:{...}, error:"stage: msg" on failure}. Upload stats do NOT include per-batch breakdown or writer count — derive client-side from filenames (src/utils/statementFilenames.js mirrors backend filename_parser.py + sorter catalog inference).
- Backend admin gate: require_admin checks ADMIN_EMAILS env var on the BACKEND — a valid token alone 403s if the email isn't listed there (matters for FE-006 smoke).
- Live-screen pattern (established in FE-002): keep old mock JSX as Mock<X> component untouched, add Live<X> component, default export switches on statementsLive. Poll with useEffect([phase, id, pollPaused]) + setInterval(2s); on poll error set pollPaused → banner with Retry that just clears pollPaused.
- Running the backend locally: ALWAYS verax_backend/venv/bin/python (system python3 lacks pdfplumber/uvicorn — start_backend.sh is broken for this). `ENVIRONMENT=DEVELOPMENT ADMIN_EMAILS="steven@verax.app,demo@demo.local" venv/bin/python -m uvicorn app.main:app --port 8000` + worker `venv/bin/python scripts/run_ingest_worker.py`. Dev DB = sqlite tunescan_development.db; if it predates statement migrations, pipeline fails with `no such column: statement.before_tax` → drop statement-domain tables + rerun create_tables() (create_all never ALTERs). Dev admin token can be minted offline: HS256 JWT {sub:"stevenhj", id:1, email:"steven@verax.app", exp} signed with SECRET_KEY from .env.development (see smoke-statements.sh).
- Real backend response shapes (verified live in FE-006): listBatches → bare array of BatchSummary {id,label,period_code,catalog,cadence,status,uploaded_at,statement_count,stats:null} — NO finding counts (batches-list badges show "—"; backend-side improvement noted). getBatch → BatchDetail + finding_counts{blocker,warning,info} + last_run. Findings/statements lists → bare arrays. Lines → {statement_id,page,page_size,total,items}. Acknowledge keeps status 'open' and only stamps acknowledged_at/by — UI must gate the button on acknowledgedAt, not status.
- Bash smoke gotcha: `cmd | python3 - <<'PY'` does NOT work — the heredoc replaces the piped stdin (program reads from stdin, then json.load gets EOF). Use `cmd | python3 -c '...'`.
---

## FE-001 — Feature flag + statements API client module (2026-06-11)
- Created branch ralph/statement-admin-ui from ralph/admin-vertical-slice; committed carried-over demo WIP first as its own chore commit (per CLAUDE.md).
- Added src/config/featureFlags.js (statementsLive) and src/api/statementsAdmin.js: createUpload (multipart FormData, onProgress percent callback), getUpload, listBatches, getBatch, listFindings, waiveFinding, acknowledgeFinding, listBatchStatements, getStatement, getStatementLines (page/page_size, default 50), revalidateBatch.
- 17 unit tests in src/api/__tests__/statementsAdmin.test.js (URL/method mapping, auth header, FormData + progress, error normalization) — green via CI=true npx react-scripts test --watchAll=false.
- Unblocked the lint gate for everyone: added .eslintignore + prettier-fixed tailwind.config.js. npm run lint now exits 0.
- No UI changes — nothing to spot-check in the browser this iteration.

## FE-002 — Upload screen: dump loose files, watch sort progress (2026-06-12)
- AdminStatementUpload.jsx restructured: MockStatementUpload (flag off, byte-for-byte old behavior) + LiveStatementUpload (flag on), shared shell/header.
- Live flow: multi-file drop zone (drag-drop + multi-select, .pdf/.xlsx, dedupe by name) → queued count + total size → createUpload with % progress bar → 202 shows Upload #id → poll getUpload every 2s → stage stepper (Queued/Sorting/Parsing/Validating/Done) + counters (files received / statements sorted / parsed / failed / remaining) → stops on done/failed.
- Sort summary on done: headline "N files → B batches → W writers"; per-batch table (label/period/catalog/statement count) derived client-side from filenames via new src/utils/statementFilenames.js (server stats only carry aggregates); unsortable files always listed with reasons (skipped=not pdf/xlsx, unparseable=filename not recognized, duplicates, unpaired=missing counterpart); failed pipeline shows stats.error verbatim.
- Errors: createUpload failure → banner (Backend unreachable on status 0) + Retry resubmits; poll failure → pauses polling + banner, Retry resumes.
- Checks: npm run lint exit 0 (no warnings in changed files), curl :3001 → 200, npm run build exit 0 (/tmp/verax_rd_frontend.log still doesn't exist).
- SPOT-CHECK route: http://localhost:3001/admin/statements/upload (needs REACT_APP_STATEMENTS_LIVE=1 for live UI; flag off must look identical to before).

## FE-003 — Batches list wired to live API (2026-06-12)
- AdminStatements.jsx restructured per the FE-002 pattern: MockStatements (flag off, identical demo behavior — old chip filters + statements table) + LiveBatches (flag on), shared shell/header (title becomes "Batches" when live).
- LiveBatches: listBatches({status, period}) refetched on filter change (query params per PRD §9 GET /admin/batches?period=&status=); columns Batch/Period/Catalog/Statements/Status chip/Findings (blocker red + warning amber badges)/Uploaded; row click → /admin/statements/:batchId (route already registered in index.js).
- Status dropdown covers the full BatchStatus lifecycle (uploaded|parsing|parsed|validating|needs_review|approved|distributed); period dropdown options accumulate from every response seen (so filtering doesn't collapse the option list).
- Loading skeleton rows (pulse animation), empty state ("No batches yet"), unreachable/error banner with Retry (refreshKey bump). Backend list endpoint does NOT exist yet (statements_admin.py has only uploads/revalidate/get-upload) — extractBatchRows tolerates bare array or {batches|items:[...]}; normalizeBatch probes statement_count/blockers/warnings in body or stats/stats.validate. RECONCILE shapes when backend lands the endpoint.
- Checks: npm run lint exit 0 (0 errors, 82 pre-existing warnings), curl :3001 → 200, npm run build exit 0 (/tmp/verax_rd_frontend.log still doesn't exist).
- SPOT-CHECK route: http://localhost:3001/admin/statements (flag on → live batches list w/ unreachable banner if backend down; flag off → identical old mock table).

## FE-004 — Batch detail: validation findings with waive/acknowledge (2026-06-12)
- AdminStatementDetail.jsx restructured per the FE-002/003 pattern: MockStatementDetail (flag off — old hardcoded BLOCKING_ISSUES/WARNING_ISSUES demo untouched) + LiveBatchDetail (flag on), shared isAdmin gate in the default export.
- LiveBatchDetail (/admin/statements/:batchId): getBatch + listFindings via Promise.allSettled (batch 404 → Batch Not Found page; findings 404 tolerated so a half-built backend doesn't kill the screen). Header: label, lifecycle status pill, period, statement count, uploaded date; summary bar = OPEN finding counts by severity (blocker/warning/info).
- Findings grouped by rule_id prefix → File integrity (V-FILE) / Statement math (V-STMT) / Ledger continuity (V-LEDG) / Batch completeness (V-BATCH) / Other; rows show severity badge (red/amber/gray), mono rule_id, message, scope_ref, status; sorted open-first then blocker>warning>info.
- Waive: modal with REQUIRED reason textarea (submit disabled while empty), waiveFinding → refetch. Acknowledge button on open warnings → acknowledgeFinding → refetch. Waived/resolved rows de-emphasized + struck-through with Details expander showing waived_by/waived_at/waived_reason.
- Revalidate button → revalidateBatch then re-polls batch+findings every 2s until status leaves 'validating' (min 2 polls, cap 30); spinner on the button while polling.
- Findings normalization tolerant: bare array or {findings|items}; waived_by_email preferred over waived_by id. Backend has NO batch/findings GET endpoints yet (statements_admin.py: uploads, get-upload, revalidate only) — RECONCILE shapes when they land. FindingStatus enum backend-side is open|resolved|waived (no 'acknowledged' yet — ack flow may need backend story).
- Checks: npx eslint on changed files clean, npm run lint exit 0 (0 errors, 82 pre-existing warnings), curl :3001 → 200, npm run build exit 0 (/tmp/verax_rd_frontend.log still doesn't exist).
- SPOT-CHECK route: http://localhost:3001/admin/statements/1 (flag on → live batch detail, error banner if backend down; flag off → identical old mock statement detail).

## FE-005 — Batch statements table + statement drill-down (2026-06-12)
- LiveBatchDetail load() now also fetches listBatchStatements(id) in the same Promise.allSettled; 404 tolerated (endpoint still unbuilt backend-side) → "Statement list isn't available from the backend yet" note instead of an error.
- Statements table below findings: Account (mono), Writer, Calculated, Payable, zero-pay reason badge (paid green / threshold_carryover blue / recouped violet / zero_earnings gray — PRD §2.6: zero is normal, explain don't alarm), detail_sum vs calculated indicator (✓ match within $0.01, else red Δ amount, — when either null), parse status colored text. Client-side search box filters by account code or writer name.
- Row click → slide-over drill-down (no new route): getStatement → account-summary waterfall per PRD §2.5 (calculated → −recouped → −reserve_taken → +reserve_released → +carried_forward_in → +payable_prev → −settlement_paid → Payable total row), each non-null component on its own row, null skipped; extras shown when present: zero-pay badge, carried_forward_out, cheque_amount, detail_sum w/ match check + line_count, parse_error.
- Lines: getStatementLines(id, page, 50) server-side pagination — Prev/Next + "Page X of Y · N lines" when the response carries total ({lines|items, total|total_count|count} or bare array); without a total, Next stays enabled while pages come back full. Only the current page is ever rendered (10k+ lines safe).
- Field names matched to backend app/models/statements.py Statement/StatementLine columns (calculated, recouped, reserve_taken/released, carried_forward_in/out, payable_prev, settlement_paid, payable, cheque_amount, detail_sum, line_count, zero_pay_reason, parse_status; lines: song_code/asset_id/custom_id, song_title, country, income_source/channel, income_type, units, earnings). toNumber() handles Numeric-as-string serialization. Statement row probes account_code/writer_name flat or nested (account.*, writer.canonical_name) — RECONCILE when backend US-010 lands the serializers.
- Checks: npx eslint --fix on changed files, npm run lint exit 0 (0 errors, 82 pre-existing warnings, none in changed files), curl :3001 → 200, npm run build exit 0 (/tmp/verax_rd_frontend.log still doesn't exist).
- SPOT-CHECK route: http://localhost:3001/admin/statements/1 (flag on) — statements table under findings; click a row for the waterfall slide-over + paginated lines. Flag off → old mock detail unchanged.

## FE-006 — attempt skipped (2026-06-12)
- Only FE-006 remains. Gate check: verax_backend scripts/ralph/prd.json US-010 (Read APIs + waiver workflow + e2e test) is still passes:false (backend at US-009, commit 60b6c9a). Per story notes, ended iteration WITHOUT marking passes — retry next iteration.

## FE-006 — Live end-to-end smoke against the real backend (2026-06-12)
- Gate cleared: backend US-010 (Read APIs + waiver workflow) landed as cf9450d mid-iteration — all PRD §9 endpoints now real; frontend client paths matched 1:1, no URL changes needed.
- Started backend (venv/bin/python -m uvicorn, ADMIN_EMAILS=steven@verax.app,demo@demo.local) + ingest worker (INGEST_POLL_SECONDS=2) in background. Dev DB had a STALE statement table (pre-before_tax migration) → pipeline failed; dropped statement-domain tables + create_tables() fixed it (documented in SMOKE.md).
- scripts/ralph/smoke-statements.sh (new): mints dev JWT offline, uploads the 14 real fixtures via curl -F, polls every 2s to done (~8s with worker), asserts 5 batches / 7 statements / 0 open blockers, C00650 payable 45,193.21 + writer "El Taiger", C00739-New threshold_carryover, C00650 carried_forward_in 38,529.94 + detail_sum≈calculated, lines page 1 (total 4,674 @ 50/page), waive round-trip when an open blocker/warning exists (none on this clean fixture set — skipped gracefully). SMOKE PASSED; rerun-safe (2nd run dedupes: still 5/7).
- Contract fixes frontend-side: acknowledge keeps status 'open' backend-side (metadata-only per PRD §5) → FindingRow now maps acknowledged_at and renders "Acknowledged ✓" instead of re-showing the button. Everything else already matched (FE-003/004/005 probes covered the real shapes).
- Backend-side notes (not fixed here): BatchSummary carries no finding counts → batches-list Findings column shows "—" in live mode (suggest adding finding_counts to GET /admin/statements/batches); fixture set produces 0 findings so the UI waive path needs a dirty fixture to exercise visually (API path proven in backend tests + smoke).
- scripts/ralph/SMOKE.md (new): start commands, gotchas (venv python, stale DB, backend ADMIN_EMAILS), scripted smoke usage, and the 6-step human click-path (upload → batches → PUB26H1/YT → C00650 waterfall → lines → waive/flag-off).
- Checks: npx eslint on changed file clean, npm run lint exit 0 (0 errors, pre-existing warnings only), curl :3001 → 200, npm run build exit 0.
- SPOT-CHECK: follow SMOKE.md §3 (needs REACT_APP_STATEMENTS_LIVE=1 + backend/worker up).
